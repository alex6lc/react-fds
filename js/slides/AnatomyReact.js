var React = require('react');

module.exports = React.createClass({
    render() {
        return (
            <section>
                <h2>Anatomy of a React Application</h2>
            </section>
        );
    }
});