var React = require('react');

module.exports = React.createClass({
    render() {
        return (
            <section>
                <h3>Further Reading...</h3>

                <p className="fragment">React Native</p>
                <p className="fragment">Om - A ClojureScript library over Facebook's React</p>
                <p className="fragment">Flux - Application architecture for building user interface</p>
                <p className="fragment">Flow - A static type checker for JavaScript</p>
            </section>
        );
    }
});