var React = require('react');

module.exports = React.createClass({
    render() {
        return (
            <section>
                <h2>Props and State</h2>
            </section>
        );
    }
});