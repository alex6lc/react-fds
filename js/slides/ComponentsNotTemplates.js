var React = require('react');

module.exports = React.createClass({
    render() {
        return (
            <section>
                <h2>Build <em>components</em>, not templates</h2>
            </section>
        );
    }
});